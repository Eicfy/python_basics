#The goal here is to define a function that, if the first set includes the second set, prints their common elements. 
#If the first set does not include the second set, it prints the difference of the second set from the first set.

set1 = set(["data", "python"])
set2 = set(["data", "function", "qcut", "lambda", "python", "miuul"])

def set(set1,set2):
    if set1.issuperset(set2):
        print(set1.intersection(set2))
    else:
        print(set2.difference(set1))

set(set1,set2)
